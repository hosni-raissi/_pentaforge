# PentaForge RAG Knowledge Base — Dependencies
# Install: pip install -r requirements.txt

# Core
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
structlog>=23.1,<25.0

# Async HTTP
httpx>=0.25,<1.0

# Embedding (local, no API key needed)
sentence-transformers>=2.2,<4.0

# Vector DB (Qdrant)
qdrant-client>=1.9,<2.0


# Cache (Redis)
redis>=5.0,<6.0
langgraph
# HTML parsing (website/gitbook extractors)
beautifulsoup4>=4.12,<5.0
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# Optional: OpenAI embeddings (if KNOWLEDGE_EMBEDDING_PROVIDER=openai)
# openai>=1.6,<2.0
# Optional: Nomic MegaBlocks acceleration (requires torch/toolchain)
# git+https://github.com/nomic-ai/megablocks.git
