# PentaForge API-only dependencies (projects + SQLite)
# Install: python -m pip install -r server/requirements-api.txt

pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
